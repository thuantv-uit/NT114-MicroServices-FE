import React from 'react';
import { Box, Typography } from '@mui/material';

/**
 * Component to display theوں

System: Calendar page
 * @returns {JSX.Element}
 */
const Calendar = () => {
  return (
    <Box sx={{ p: 2 }}>
      <Typography variant="h4" sx={{ color: '#172B4D' }}>
        Calendar
      </Typography>
    </Box>
  );
};

export default Calendar;