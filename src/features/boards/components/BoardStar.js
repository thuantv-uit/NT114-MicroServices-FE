import React from 'react';
import { Typography } from '@mui/material';

/**
 * Placeholder component for the Start page
 * @returns {JSX.Element}
 */
const BoardStar = () => {
  return (
    <Typography variant="h4" sx={{ p: 2 }}>
      Start Page
    </Typography>
  );
};

export default BoardStar;