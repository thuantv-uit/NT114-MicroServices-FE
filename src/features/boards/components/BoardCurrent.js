import React from 'react';
import { Typography } from '@mui/material';

/**
 * Placeholder component for the Current page
 * @returns {JSX.Element}
 */
const BoardCurrent = () => {
  return (
    <Typography variant="h4" sx={{ p: 2 }}>
      Current Page
    </Typography>
  );
};

export default BoardCurrent;