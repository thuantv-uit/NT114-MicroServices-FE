version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    image: nt114-microservice-fe:latest
    ports:
      - "3000:3000"
    restart: unless-stopped

# Định nghĩa network
networks:
  app-network:
    driver: bridge